version: '3.7'

services:
  client:
    image: node:20
    volumes:
      - ./:/app
    working_dir: /app
    environment:
      VITE_HOST: 0.0.0.0
      VITE_PORT: 3000
    command: bash -c "yarn install && yarn dev"
    ports:
      - 3000:3000
    tty: true
